{
  "name": "find-your-anime",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack anime discovery platform with React, Express, MongoDB, and JWT authentication.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
